version: '3'

services:

  wikipedia:
    image: lambdacube/wikipedia-api:2.0.0
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/usr/share/wikipedia/logs
      - ./conf:/usr/share/wikipedia/conf
